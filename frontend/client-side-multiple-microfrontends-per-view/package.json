{
  "name": "client-side-multiple-microfrontends-per-view",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "cd microfrontends/user && npm install && npm run build && cd ../carts && npm install && npm run build && cd ../catalogue && npm install && npm run build"
  }
}
